// const webpack = require('webpack');

module.exports = config => {
  // config.plugins = [envVariables()];
};

// TODO: this plugin throws errors?
// const envVariables = () => new webpack.EnvironmentPlugin(['BASE_URL']);

// Webpack bootstrap logic
// https://webpack.js.org/plugins/commons-chunk-plugin/#manifest-file
// const webpackBootstrap = () =>
//   new webpack.optimize.CommonsChunkPlugin({
//     name: 'manifest',
//     minChunks: Infinity,
//   });
